{
  "name": "csf-eligibility-portal",
  "version": "1.0.0",
  "description": "Password-gated portal that scores CSF applicants from their Aeries report cards.",
  "scripts": {
    "test": "node scripts/test-scoring.mjs && node scripts/test-send.mjs",
    "dev": "wrangler pages dev",
    "db:create": "wrangler d1 create csf-eligibility-settings",
    "db:init": "wrangler d1 execute csf-eligibility-settings --local --file=schema.sql && wrangler d1 execute csf-eligibility-settings --local --file=seed.sql",
    "db:init:remote": "wrangler d1 execute csf-eligibility-settings --remote --file=schema.sql && wrangler d1 execute csf-eligibility-settings --remote --file=seed.sql",
    "deploy": "wrangler pages deploy"
  },
  "type": "module",
  "dependencies": {
    "pdfjs-dist": "^4.10.38"
  },
  "private": true,
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
